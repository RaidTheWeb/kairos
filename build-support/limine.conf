timeout: 5

${KERNEL}=boot():/nomos

/Kairos
    protocol: limine
    path: ${KERNEL}
    module_path: boot():/initramfs.tar.lz4
    cmdline: mmsan nzalloc initramfs="/initramfs.tar.lz4" root="initramfs" init="/bin/init"

/Kairos - No SMP
    protocol: limine
    path: ${KERNEL}
    module_path: boot():/initramfs.tar.lz4
    cmdline: nosmp mmsan nzalloc initramfs="/initramfs.tar.lz4" root="initramfs"
